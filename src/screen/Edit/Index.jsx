import {Text, View} from "react-native";

const Edit = (props)=>{
    const {id} = props?.route.params
    return (
        <View>
            <Text>Edit</Text>
        </View>
    )
}
export default Edit